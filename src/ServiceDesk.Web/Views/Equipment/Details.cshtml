@model EquipmentDto
@{
    ViewData["Title"] = $"Оборудование: {Model.Model}";
    var history = ViewBag.History as IEnumerable<EquipmentHistoryDto>
        ?? Enumerable.Empty<EquipmentHistoryDto>();
}

<div class="mb-3">
    <a href="/Equipment/Index" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> К списку
    </a>
</div>

<div class="row g-4">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header"><h5 class="mb-0">@Model.Model</h5></div>
            <div class="card-body">
                @if (!string.IsNullOrEmpty(Model.PhotoPath))
                {
                    <div class="mb-3 text-center">
                        <img src="@Model.PhotoPath" alt="Фото оборудования"
                             style="max-width:100%;max-height:300px;border-radius:8px;object-fit:contain;" />
                    </div>
                }
                @if (Model.IsCompanyOwned)
                {
                    <div class="mb-3">
                        <span class="badge bg-info">
                            <i class="bi bi-arrow-repeat"></i> Подменное оборудование
                        </span>
                        <div class="form-text">Собственность компании, используется для подмены на период ремонта</div>
                    </div>
                }
                <div class="mb-3">
                    <small class="text-muted">Серийный номер</small>
                    <p class="mb-0 fw-bold">@Model.SerialNumber</p>
                </div>
                <div class="mb-3">
                    <small class="text-muted">Точка обслуживания</small>
                    <p class="mb-0">@Model.ServicePointName</p>
                    @if (!string.IsNullOrEmpty(Model.ServicePointAddress))
                    {
                        <a href="https://yandex.ru/maps/?text=@(Uri.EscapeDataString(Model.ServicePointAddress))"
                           target="_blank" rel="noopener noreferrer" class="text-decoration-none">
                            <small class="link-primary"><i class="bi bi-geo-alt"></i> @Model.ServicePointAddress</small>
                        </a>
                    }
                    else
                    {
                        <small class="text-muted"><i class="bi bi-geo-alt"></i> Адрес не указан</small>
                    }
                </div>
                @if (Model.InstalledAt.HasValue)
                {
                    <div class="mb-3">
                        <small class="text-muted">Дата установки</small>
                        <p class="mb-0">@Model.InstalledAt.Value.ToString("dd.MM.yyyy")</p>
                    </div>
                }
                @if (!string.IsNullOrEmpty(Model.Description))
                {
                    <div>
                        <small class="text-muted">Описание</small>
                        <p class="mb-0">@Model.Description</p>
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h6 class="mb-0"><i class="bi bi-clock-history"></i> История ремонта</h6>
            </div>
            <div class="card-body">
                @if (!history.Any())
                {
                    <p class="text-muted text-center">Нет записей</p>
                }
                else
                {
                    @foreach (var record in history)
                    {
                        <div class="d-flex justify-content-between align-items-start border-bottom py-2">
                            <div>
                                <a href="/Tickets/Details/@record.TicketId" class="text-decoration-none fw-bold">
                                    @record.TicketNumber
                                </a>
                                <p class="small text-muted mb-0">@record.Description</p>
                            </div>
                            <small class="text-muted text-nowrap">@record.CreatedAt.ToString("dd.MM.yy")</small>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
